Implement complete patient-doctor interaction system for MindMate.

CURRENT ISSUES TO FIX:
- Patient dashboard should show ALL available doctors (not just appointments)
- Patient should be able to open doctor profile from dashboard
- Patient should be able to book appointment OR send direct message from doctor card
- Doctor dashboard should show patients who interacted (appointments + messages)
- Doctor should see unified inbox of patient requests

REQUIREMENTS:

===========================================
PART 1: PATIENT DASHBOARD (client/src/pages/dashboard/patient.tsx)
===========================================
Add "Find Doctors" section that shows:
- Grid of ALL available doctor cards (fetch from GET /api/doctors)
- Each card shows: doctor name, specialization, fee, rating, profile picture
- Each card has TWO buttons:
  1. "Book Appointment" - opens appointment dialog
  2. "Message" - navigates to /chat/:doctorId
- Search/filter bar on top (by specialization, name, fee range)
- Below that, show "My Appointments" section (existing)

===========================================
PART 2: DOCTOR DASHBOARD (client/src/pages/dashboard/doctor.tsx)
===========================================
Add "Patient Interactions" section showing:
- List of ALL patients who have:
  - Booked appointments with this doctor
  - Sent messages to this doctor
- Each patient card shows:
  - Patient name, profile picture, age (if available)
  - Last interaction date
  - Unread message count (if any)
  - Status: "Has pending appointment" / "Active chat" / etc.
- Two action buttons per patient:
  1. "View Chat" - opens /chat/:patientId
  2. "View Appointments" - shows patient's appointment history

Keep existing appointment requests section (pending accept/reject)

===========================================
PART 3: UNIFIED CHAT SYSTEM
===========================================
- Chat page at /chat/:userId works for both patient and doctor
- Shows full conversation history
- Real-time messages via Socket.io (already implemented)
- Typing indicator
- Message delivered/read status

===========================================
PART 4: APPOINTMENT FROM DOCTOR CARD
===========================================
- Clicking "Book Appointment" opens dialog
- Fetches available time slots for that doctor
- Patient selects date, time slot, reason
- Submits to POST /api/appointments
- Shows success message and refreshes appointment list

===========================================
PART 5: API ENHANCEMENTS (if needed)
===========================================
- GET /api/doctors/patient-view - Returns all doctors with interaction status for logged-in patient
- GET /api/patients/doctor-view - Returns all patients who interacted with logged-in doctor
- Include unread message counts in both responses

===========================================
USER FLOW SUMMARY:
===========================================
Patient Login → Dashboard sees ALL doctors → Clicks doctor card → Book OR Message
Doctor Login → Dashboard sees ALL patients who interacted → Click patient → Chat OR View appointments

IMPLEMENTATION STYLE:
- Use existing Tailwind CSS classes
- Use shadcn/ui components (Card, Button, Avatar, Badge)
- Use existing use-doctors.ts and use-appointments.ts hooks
- Create new useChatInteractions.ts hook if needed

After complete implementation, say "Patient-Doctor interaction system complete. Patients can see all doctors and message/book directly. Doctors see all interacting patients."
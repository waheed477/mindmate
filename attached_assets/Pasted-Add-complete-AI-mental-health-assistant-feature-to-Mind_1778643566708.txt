Add complete AI mental health assistant feature to MindMate with proper UI and user flow.

REQUIREMENTS:

1. BACKEND API (server/routes/ai-chat.ts):
   - POST /api/ai/chat - Accept { message, userId }
   - Use Transformers.js with Kenko model (IniNLP247/Kenko-mental-health-llama-3-model)
   - Model loads once, caches for subsequent requests
   - Return { response, timestamp }

2. REGISTER ROUTE IN server/index.ts:
   - import aiChatRoutes from './routes/ai-chat.js'
   - app.use('/api/ai', aiChatRoutes)

3. FRONTEND PAGE (client/src/pages/AIAssistant.tsx):
   - Requires authentication (patient only)
   - Chat interface with:
     - Message bubbles (user messages right, AI left)
     - Welcome message: "Hello! I'm MindMate AI Assistant. How can I support you today?"
     - 6 suggested question buttons above input:
       1. "I've been feeling anxious all week"
       2. "I can't stop negative thoughts"
       3. "How do I cope with stress?"
       4. "I'm struggling with sleep"
       5. "I feel lonely and isolated"
       6. "How to build self-confidence?"
     - Text input with send button
     - Loading indicator while AI responds
     - Typing indicator when AI is thinking
     - Disclaimer at bottom: "⚠️ This AI provides general mental health support. For emergencies, please call 988 or your local crisis helpline."

4. ADD ROUTE IN App.tsx:
   - import AIAssistant from "./pages/AIAssistant"
   - <Route path="/ai-assistant" element={<ProtectedRoute allowedRole="patient"><AIAssistant /></ProtectedRoute>} />

5. ADD NAVBAR BUTTON (client/src/components/layout-navbar.tsx):
   - Add "AI Assistant" link in patient navbar
   - Icon: MessageCircle or Bot

6. ADD CRISIS HELPLINE SIDEBAR:
   - On right side of chat page
   - Show: National Suicide Prevention Lifeline: 988
   - Show: Crisis Text Line: TEXT "HOME" to 741741

USER FLOW:
1. Patient logs in → Dashboard
2. Clicks "AI Assistant" in navbar
3. Sees chat page with suggested questions
4. Clicks a question OR types own message
5. AI responds within 5-10 seconds
6. Conversation continues

TECHNICAL:
- Use @huggingface/transformers library
- Model loads on first request (takes 1-2 minutes, show loading spinner)
- Cache model after loading
- No API keys required

After implementation, say "AI Assistant ready. Patient can access from navbar after login."